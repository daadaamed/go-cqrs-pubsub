# Copy to .env and adjust if needed. Loaded by the app in later phases.
DATABASE_URL=postgres://todo:todo@localhost:5432/todo?sslmode=disable
PUBSUB_EMULATOR_HOST=localhost:8085
PUBSUB_PROJECT_ID=todo-local
PORT=8080